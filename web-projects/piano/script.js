/* ============================================================
   Piano Chords — shared script
   Handles the mobile sidenav open/close.
   ============================================================ */

function openNav() {
  document.querySelector(".sidenav").classList.add("open");
}
function closeNav() {
  document.querySelector(".sidenav").classList.remove("open");
}
